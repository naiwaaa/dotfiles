#!/usr/bin/bash

{{-
  $packages := list
    "pavucontrol"
    "pipewire"
    "playerctl"
    "wireplumber"
    "pipewire-alsa"
    "pipewire-audio"
    "pipewire-pulse"
-}}

{{ "" }}

paru -S {{ $packages | join " " }}
